# คู่มือการใช้งานและบันทึกคำสั่ง Git - โปรเจกต์ฟาร์มปู (Crab Farm System)

คู่มือฉบับนี้จัดทำขึ้นเพื่อบันทึกขั้นตอนและคำสั่งพื้นฐานในการอัปเดตโค้ดและส่งข้อมูลขึ้นไปยังระบบควบคุมเวอร์ชัน (Git/GitHub) สำหรับโปรเจกต์ฟาร์มปู เพื่อให้ทีมงานหรือตัวคุณเองสามารถทบทวนและใช้งานได้อย่างรวดเร็ว

---

## 1. คำสั่งพื้นฐานสำหรับการอัปเดตโค้ด (Git Workflow)

เมื่อมีการแก้ไขหรือเพิ่มฟีเจอร์ใหม่ในระบบ ให้ใช้ชุดคำสั่งหลัก 3 คำสั่งนี้ผ่าน Terminal (Command Line):

```bash
# ขั้นตอนที่ 1: เพิ่มไฟล์ทั้งหมดที่มีการเปลี่ยนแปลงเข้าสู่ Staging Area
git add .

# ขั้นตอนที่ 2: บันทึกประวัติการแก้ไข (Commit) พร้อมข้อความอธิบายการเปลี่ยนแปลง
git commit -m "อัปเดตฟีเจอร์ใหม่ของฟาร์มปู"

# ขั้นตอนที่ 3: ส่งโค้ดขึ้นไปยัง Repository หลักบน Remote (เช่น GitHub)
git push origin main
```

---

## 2. คำอธิบายรายละเอียดแต่ละคำสั่ง

* **`git add .`**
  * **หน้าที่:** สแกนหาไฟล์ทั้งหมดที่มีการสร้างใหม่, แก้ไข หรือลบในโปรเจกต์ และเตรียมความพร้อมไฟล์เหล่านั้นเพื่อบันทึกประวัติ
  * **เกร็ดความรู้:** เครื่องหมายจุด (`.`) หมายถึงเลือกทุกไฟล์ในโฟลเดอร์ปัจจุบัน

* **`git commit -m "ข้อความอธิบาย"`**
  * **หน้าที่:** ทำการบันทึก (Snapshot) โค้ดชุดปัจจุบันเก็บไว้ในประวัติท้องถิ่น (Local Repository)
  * **คำแนะนำ:** ข้อความในเครื่องหมายคำพูด `-m` ควรสั้นและสื่อถึงสิ่งที่แก้ไข เช่น `"เพิ่มระบบคำนวณอาหารปู"` หรือ `"แก้ไขหน้าแดชบอร์ดแสดงผล"` เพื่อให้อ่านย้อนหลังได้ง่าย

* **`git push origin main`**
  * **หน้าที่:** อัปโหลดประวัติและโค้ดทั้งหมดที่เรา Commit ไว้ ส่งตรงไปยังเซิร์ฟเวอร์หลัก (เช่น GitHub) บน Branch ที่ชื่อว่า `main`

---

## 3. ข้อควรระวังและทริคเพิ่มเติม (Best Practices)

1. **เช็กสถานะก่อนทำงานเสมอ:** ก่อนจะพิมพ์คำสั่งใดๆ แนะนำให้พิมพ์ **`git status`** เพื่อดูว่ามีไฟล์อะไรบ้างที่ยังไม่ได้บันทึก
2. **ดึงข้อมูลล่าสุดก่อนทำงานร่วมกับผู้อื่น:** หากทำโปรเจกต์ร่วมกับผู้อื่น ควรพิมพ์ **`git pull`** ก่อนเริ่มเขียนโค้ดหรือก่อนทำการ Push เพื่อป้องกันปัญหา Conflict (โค้ดตีกัน)
3. **การตั้งชื่อ Branch:** หากกำลังพัฒนาฟีเจอร์ใหม่ที่ยังไม่เสถียร แนะนำให้สร้าง Branch แยกต่างหาก เช่น `git checkout -b feature/crab-feeding` แทนการลงใน `main` โดยตรง
