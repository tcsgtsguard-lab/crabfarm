# คู่มือเอาแอป Andaman Crab Farm ขึ้นออนไลน์ (ฉบับละเอียด)

แอปตัวนี้เขียนมาให้ทำงานได้ 2 แบบอยู่แล้ว:
1. ในกล่อง Artifact ของ Claude.ai (ใช้ `window.storage`)
2. เมื่อ deploy ขึ้นเว็บจริง จะใช้ **Supabase** เป็นฐานข้อมูลกลาง เพื่อให้ทุกคน/ทุกอุปกรณ์เห็นข้อมูลชุดเดียวกัน

ด้านล่างนี้คือขั้นตอนทำทั้งหมด ตั้งแต่สร้างฐานข้อมูล ไปจนถึงได้ลิงก์เว็บไซต์จริงที่เปิดจากมือถือ/คอมพิวเตอร์เครื่องไหนก็ได้

ใช้เวลาทั้งหมดประมาณ 20-30 นาที ไม่ต้องมีพื้นฐานเขียนโปรแกรมมาก่อนก็ทำตามได้ (แค่ก็อปวาง)

---

## ภาพรวม 3 ขั้นตอนใหญ่

1. **สร้างฐานข้อมูลบน Supabase** (ฟรี) — เก็บข้อมูลกล่องปู/ประวัติ/ค่าน้ำ
2. **อัปโค้ดขึ้น GitHub** — เพื่อให้ Vercel ดึงไป build
3. **Deploy ขึ้น Vercel** (ฟรี) — ได้ลิงก์เว็บไซต์จริง เช่น `andaman-crab-farm.vercel.app`

---

## ขั้นตอนที่ 1 — สร้างฐานข้อมูล Supabase

### 1.1 สมัครและสร้างโปรเจกต์
1. ไปที่ https://supabase.com แล้วกด **Start your project** → สมัครด้วย GitHub หรืออีเมล
2. กด **New project**
3. ตั้งชื่อโปรเจกต์ เช่น `andaman-crab-farm`
4. ตั้งรหัสผ่านฐานข้อมูล (Database Password) — จดเก็บไว้ (เผื่อใช้ภายหลัง แต่ขั้นตอนนี้ไม่จำเป็นต้องใช้รหัสนี้โดยตรง)
5. เลือก Region ใกล้ไทยที่สุด เช่น **Southeast Asia (Singapore)**
6. กด **Create new project** แล้วรอประมาณ 1-2 นาทีให้ระบบตั้งค่าเสร็จ

### 1.2 สร้างตารางเก็บข้อมูล
1. ในเมนูซ้าย เลือก **SQL Editor**
2. กด **New query**
3. เปิดไฟล์ `supabase-setup.sql` ที่แนบมาให้ในชุดนี้ คัดลอกทั้งหมด แล้ววางในช่อง SQL Editor
4. กด **Run** (มุมขวาล่าง)
5. ควรเห็นข้อความ "Success. No rows returned" แปลว่าสร้างตารางสำเร็จ

> ตารางนี้ชื่อ `crabfarm_kv` เป็นตารางเดียวที่แอปทั้งหมดใช้เก็บข้อมูลทุกอย่าง (กล่องปู/ประวัติ/ค่าน้ำ/การตั้งค่า) แอปจะจัดการโครงสร้างข้อมูลภายในเอง ไม่ต้องสร้างตารางเพิ่ม

### 1.3 คัดลอก URL และ Anon Key
1. ในเมนูซ้าย เลือก **Project Settings** (ไอคอนเฟือง) → **API**
2. คัดลอกค่า 2 ตัวนี้เก็บไว้ (จะใช้ในขั้นตอนที่ 2):
   - **Project URL** — หน้าตาเหมือน `https://xxxxxxxxxxxx.supabase.co`
   - **anon public key** — เป็นข้อความยาวๆ ขึ้นต้นด้วย `eyJ...`

> Anon key นี้ปลอดภัยที่จะฝังไว้ในโค้ดฝั่งเว็บ (public) ได้ เพราะ Supabase ออกแบบมาให้ใช้แบบนี้ — สิทธิ์การเข้าถึงจริงๆ ถูกควบคุมด้วย Row Level Security ที่ตั้งไว้ในขั้นตอน 1.2

---

## ขั้นตอนที่ 2 — เตรียมโค้ดและอัปขึ้น GitHub

ชุดไฟล์ที่แนบมาให้ (โฟลเดอร์ `crabfarm-deploy`) มีโครงสร้างพร้อม build แล้ว ประกอบด้วย:

```
crabfarm-deploy/
├── index.html          ← ใส่ค่า Supabase ตรงนี้
├── package.json
├── vite.config.js
├── supabase-setup.sql
├── .gitignore
└── src/
    ├── main.jsx
    └── App.jsx          ← ไฟล์แอปของคุณ (andaman-crab-farm5.jsx เดิม)
```

### 2.1 ใส่ค่า Supabase ลงในไฟล์
1. เปิดไฟล์ `index.html`
2. หาบรรทัดนี้:
   ```js
   window.CRABFARM_CONFIG = {
     supabaseUrl: "YOUR_SUPABASE_URL",
     supabaseAnonKey: "YOUR_SUPABASE_ANON_KEY",
   };
   ```
3. แทนที่ `YOUR_SUPABASE_URL` และ `YOUR_SUPABASE_ANON_KEY` ด้วยค่าที่คัดลอกไว้จากขั้นตอน 1.3 แล้วบันทึกไฟล์

### 2.2 สร้างบัญชี GitHub (ถ้ายังไม่มี)
1. ไปที่ https://github.com แล้วสมัครสมาชิก

### 2.3 สร้าง Repository ใหม่
1. กดปุ่ม **+** มุมบนขวา → **New repository**
2. ตั้งชื่อ เช่น `andaman-crab-farm`
3. เลือก **Private** (แนะนำ เพราะมีข้อมูลฟาร์ม) หรือ Public ก็ได้
4. กด **Create repository** (ไม่ต้องติ๊ก "Add README")

### 2.4 อัปโหลดไฟล์ขึ้น GitHub
วิธีง่ายที่สุดถ้าไม่ถนัดใช้คำสั่ง:
1. ในหน้า repository ที่สร้างใหม่ จะมีลิงก์ **uploading an existing file**
2. ลากทั้งโฟลเดอร์ `crabfarm-deploy` (หรือไฟล์ทั้งหมดข้างใน) ไปวางในหน้านั้น
3. เลื่อนลงมากด **Commit changes**

*(ถ้าถนัดใช้ Git ผ่าน command line แทน ให้รันคำสั่งนี้ในโฟลเดอร์ `crabfarm-deploy`)*
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/andaman-crab-farm.git
git push -u origin main
```

---

## ขั้นตอนที่ 3 — Deploy ขึ้น Vercel

1. ไปที่ https://vercel.com → กด **Sign Up** → เลือก **Continue with GitHub** (ให้เชื่อมบัญชี GitHub ที่เพิ่งใช้)
2. หลัง login เข้าหน้า Dashboard กด **Add New...** → **Project**
3. จะเห็นรายชื่อ repository ของคุณ — เลือก `andaman-crab-farm` แล้วกด **Import**
4. Vercel จะตรวจจับว่าเป็นโปรเจกต์ Vite เองอัตโนมัติ (Framework Preset: Vite) — ไม่ต้องแก้อะไร
4.1. **(ถ้าจะใช้ฟีเจอร์ "📷 ถ่ายรูปให้ AI อ่านค่าน้ำ")** ก่อนกด Deploy ให้เปิด **Environment Variables** (อยู่ในหน้าเดียวกันตอน Import) แล้วเพิ่ม:
   - Key: `GEMINI_API_KEY`
   - Value: API key ฟรีจาก **Google AI Studio** — ไปที่ https://aistudio.google.com/apikey กด **Create API key** (ไม่ต้องผูกบัตรเครดิต ใช้ฟรีได้เลย มีโควต้าต่อวัน/ต่อนาทีให้)

   ถ้าข้ามขั้นตอนนี้ แอปยังใช้งานส่วนอื่นได้ปกติ แค่ปุ่ม "ถ่ายรูปให้ AI อ่านค่า" จะขึ้น error แจ้งว่ายังไม่ได้ตั้งค่า — กลับมาเพิ่ม env var แล้ว redeploy ทีหลังได้เสมอ (ไปที่ Project > Settings > Environment Variables) ในแอปเองก็มีปุ่มลิงก์ไปสมัครฟรีให้กดตรง ๆ ได้เลยตรงหน้า "บันทึกค่าน้ำ"
5. กด **Deploy**
6. รอประมาณ 1-2 นาที เมื่อเสร็จจะมีหน้าจอแสดงลิงก์เว็บไซต์ เช่น
   `https://andaman-crab-farm.vercel.app`
7. กดเข้าลิงก์เพื่อทดสอบว่าแอปเปิดขึ้นมาและบันทึกข้อมูลได้จริง (ลองเพิ่มกล่องปูสัก 1 กล่อง แล้วรีเฟรชหน้าดูว่าข้อมูลยังอยู่)

จากนี้ทุกครั้งที่คุณแก้โค้ดแล้ว push ขึ้น GitHub ใหม่ Vercel จะ deploy เวอร์ชันใหม่ให้อัตโนมัติ

---

## การใช้งานร่วมกันหลายคน/หลายเครื่อง

เปิดลิงก์ Vercel เดียวกันจากมือถือ/คอมพิวเตอร์กี่เครื่องก็ได้ ทุกเครื่องจะเห็นและแก้ข้อมูลชุดเดียวกันผ่าน Supabase โดยอัตโนมัติ ไม่ต้องซิงก์เอง

แนะนำ: กด **เพิ่มไปหน้าจอหลัก (Add to Home Screen)** จากเบราว์เซอร์มือถือ เพื่อให้เปิดใช้เหมือนแอปได้เลย

---

## ทางเลือกเสริม (ไม่บังคับ)

- **โดเมนของตัวเอง**: ใน Vercel project ไปที่ **Settings > Domains** เพื่อผูกโดเมนที่ซื้อไว้ (เช่น `farm.yourdomain.com`)
- **จำกัดสิทธิ์การเข้าถึงข้อมูล**: ตอนนี้ตั้งไว้ให้ใครมีลิงก์ก็แก้ข้อมูลได้เลย (ไม่มี login) ถ้าต้องการความปลอดภัยเพิ่ม สามารถเพิ่ม Supabase Auth + ปรับ RLS policy ทีหลังได้ (แจ้งได้ถ้าต้องการให้ช่วยทำส่วนนี้)
- **ตั้งค่ารหัสผ่านง่ายๆ หน้าเว็บ**: ถ้าไม่อยากทำระบบ login เต็มรูปแบบ สามารถขอให้เพิ่มหน้า "ใส่รหัสผ่านก่อนเข้า" แบบง่ายๆ ในตัวแอปได้เช่นกัน

---

## แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุที่เป็นไปได้ | วิธีแก้ |
|---|---|---|
| เปิดเว็บแล้วหน้าขาว/error | ลืมใส่ค่า Supabase ใน `index.html` หรือใส่ผิด | เช็คว่า URL/Key ตรงกับใน Supabase Project Settings > API |
| กดบันทึกแล้วข้อมูลหาย | ยังไม่ได้รัน `supabase-setup.sql` หรือ RLS ปิดกั้นอยู่ | กลับไปเช็คขั้นตอน 1.2 ว่า Run สำเร็จและมี policy "allow all" |
| Vercel build ล้มเหลว | โครงสร้างไฟล์ไม่ตรง (เช่นไฟล์ไม่ได้อยู่ใน `src/`) | เช็คว่าโฟลเดอร์ที่อัปขึ้น GitHub มีโครงสร้างตรงกับที่ระบุด้านบน |

หากติดขั้นตอนไหน ส่ง screenshot ของ error มาได้เลย จะช่วยไล่ดูให้
